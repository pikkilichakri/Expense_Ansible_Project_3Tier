---
- name: configure mysql server
  hosts: mysql
  become: yes # with  root access
  tasks:
  - name: install mysql-server
    ansible.builtin.package:
      name: mysql-server
      state: installed